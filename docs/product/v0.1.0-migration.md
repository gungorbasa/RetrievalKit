# Migrating to RetrievalKit 0.1.0

`0.1.0` is the first preview, so there is no earlier package release to upgrade
from. Existing repository users should rebuild the Rust core and select the
products required by their application:

- Swift applications add the `RetrievalKit` package once and select
  `RetrievalKit`, `RetrievalKitGraph`, `EmbeddingKit`, and/or
  `RetrievalKitPipeline`. Base and graph products can coexist because they use
  one graph-capable native aggregate.
- Swift callers should replace `import RetrievalKitIngest` with
  `import RetrievalKit`; `TextChunker` is now part of the base product.
- Python applications install either `retrievalkit` or `retrievalkit-graph` and
  may independently install `retrievalkit-embedding` for local text embedding.
- Node applications select `@gungorbasa/retrievalkit` or
  `@gungorbasa/retrievalkit-graph` and may independently install
  `@gungorbasa/retrievalkit-embedding`. Browser applications use the separate
  `@gungorbasa/retrievalkit-browser-embedding` Worker package.
- Kotlin/JVM and Android applications select one retrieval aggregate and may
  independently add `io.github.gungorbasa:retrievalkit-embedding` or
  `io.github.gungorbasa:retrievalkit-embedding-android` respectively. Android
  API 24+ arm64-v8a ships as a preview: package resolution and compilation are
  retained release checks, while live-device inference, compatibility, and
  performance remain unqualified and deferred.

V1, V2, and V3 base indexes remain readable; their next save publishes a
checksummed V4 snapshot. V1 and V2 do not have payload checksums; V3 and V4 do.
Validate or back up application data before migration, and do not edit snapshot
files directly. Graph capability formats are versioned and validated
independently from the embedded base snapshot.
