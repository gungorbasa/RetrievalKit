# Migrating to VectorKit 0.1.0

`0.1.0` is the first preview, so there is no earlier package release to upgrade
from. Existing repository users should rebuild the Rust core and choose one
capability aggregate per process:

- Swift base applications use `VectorKit` and optionally `VectorKitIngest`,
  `EmbeddingKit`, and `VectorKitPipeline`.
- Swift graph applications use `VectorKitGraph` and must not also link the base
  native aggregate.
- Python applications install either `vectorkit` or `vectorkit-graph`.

Existing V1 and V2 base indexes remain readable. The next save writes the
current transactional, checksummed format. Validate or back up application data
before migration; do not edit snapshot files directly.
