# Migrating to RetrievalKit 0.1.0

`0.1.0` is the first preview, so there is no earlier package release to upgrade
from. Existing repository users should rebuild the Rust core and choose one
capability aggregate per process:

- Swift base applications select the `RetrievalKit` package and may use its
  `RetrievalKit`, `RetrievalKitIngest`, `EmbeddingKit`, and
  `RetrievalKitPipeline` products.
- Swift graph applications select the standalone `RetrievalKitGraph` package
  and must not also link the base native aggregate. The packages are separate
  so SwiftPM downloads only the selected native capability.
- Python applications install either `retrievalkit` or `retrievalkit-graph`.

Existing V1 and V2 base indexes remain readable. The next save writes the
current transactional, checksummed format. Validate or back up application data
before migration; do not edit snapshot files directly.
