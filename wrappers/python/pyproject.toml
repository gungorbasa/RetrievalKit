[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "retrievalkit"
version = "0.1.0"
description = "Thin Python wrapper for the RetrievalKit Rust retrieval core"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
  { name = "RetrievalKit" }
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Rust",
  "Topic :: Software Development :: Libraries"
]

[tool.maturin]
manifest-path = "../../crates/retrievalkit-python/Cargo.toml"
module-name = "retrievalkit._native"
python-source = "python"
exclude = ["python/**/__pycache__/**", "python/**/*.pyc"]

[tool.mypy]
python_version = "3.10"
strict = true
files = [
  "python/retrievalkit",
  "tests/typing_smoke.py",
]

[tool.ruff]
target-version = "py310"
line-length = 88
src = ["python", "tests"]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "I",
  "UP",
]
